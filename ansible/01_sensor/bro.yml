---
- hosts: all
  gather_facts: true
  remote_user: toor
  become: true
  become_method: sudo
  become_user: root
  tasks:
    - include_vars: "./var/bro_vars.yml"
    - include: ./task/bro_tasks.yml
