---
- hosts: all
  gather_facts: true
  remote_user: toor
  become: true
  become_method: sudo
  become_user: root
  tasks:
    - include_vars: "./var/pfring_vars.yml"
    - include: ./task/pfring_tasks.yml
